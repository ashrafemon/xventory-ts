import { Typography } from "@material-ui/core";
import React from "react";

const SettingContent = () => {
    return (
        <div>
            <Typography>Store Setting</Typography>
        </div>
    );
};

export default SettingContent;
