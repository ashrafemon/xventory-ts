import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles(() => ({
    pageContent: {
        backgroundColor: "#1F2129",
        padding: "30px 35px",
        position: "relative",
        borderRadius: 8,
    },
}));
